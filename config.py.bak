# Example SocketShark configuration file
# For more settings, see socketshark/config_defaults.py

# Host and port to bind WebSockets.
WS_HOST = '127.0.0.1'
WS_PORT = '3000'
#WS_SSL = {
#    'cert': '/Users/amilkowski/cert/CA/localhost/localhost.crt',
#    'key': '/Users/amilkowski/cert/CA/localhost/localhost.key',
#}

# Redis options
REDIS = {
    'host': 'localhost',
    'port': 6379,
}

# List of services
SERVICES = {
    'blocknetdx': {
        # Whether to always require authentication. When False, anonymous
        # sessions are supported even if an authorizer is configured.
        'require_authentication': False,
    },
    'crypto_signal': {
        # Whether to always require authentication. When False, anonymous
        # sessions are supported even if an authorizer is configured.
        'require_authentication': False,
    },
    'twitter_stream': {
        # Whether to always require authentication. When False, anonymous
        # sessions are supported even if an authorizer is configured.
        'require_authentication': False,
    }
}
